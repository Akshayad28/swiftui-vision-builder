String query = "SELECT COUNT(*) as cnt " +
               "FROM (" +
               "SELECT file_id, landing_rec_id " +
               "FROM LHONE_EXCEPTION_POS " +
               "WHERE FILE_ID = ? " +
               "AND TRUNC(BUSINESS_DATE) = TO_DATE(?, 'DD-MON-YYYY') " +
               "AND MONITOR_ID = ? " +
               "GROUP BY file_id, landing_rec_id" +
               ")";
